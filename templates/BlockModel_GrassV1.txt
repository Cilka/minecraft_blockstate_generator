{
   "parent": "block/grass",
   "textures": {
          "particle": "#{modId}:blocks/#{dirtType}_#{grassColor}_side",
          "bottom": "#{modId}:blocks/#{grassColor}_dirt",
	   "top": "#{modId}:blocks/#{grassColor}_grass",
	   "side": "#{modId}:blocks/#{dirtType}_#{grassColor}_side",
	   "overlay":"#{modId}:blocks/#{dirtType}_#{grassColor}_side"
   }
}