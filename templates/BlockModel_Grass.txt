{
	"parent":"block/grass",
	"textures": {
		"particle": "#{modId}:#{bottom}",
		"bottom" : "#{modId}:#{bottom}",
		"top" : "#{modId}:#{top}",
		"side" : "#{modId}:#{side}",
		"overlay": "#{modId}:#{side}"
		
	}
}